/** 환경 유형 */
export const enum ServerEnvEnum {
  Local = 'local',
  Staging = 'staging',
  Production = 'production',
}

export const enum NodeEnvEnum {
  Test = 'test',
  Development = 'development',
  Production = 'production',
}
